# Proposed Method

DHTL-Net training code and the DHTL-Net w/o KMD ablation. The long training scripts are split into entry points, runner modules, model definitions, data loading, training utilities, and artifact export helpers.
