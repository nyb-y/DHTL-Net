# Checkpoints

This directory is reserved for downloaded checkpoint files. Checkpoints are documented in `../MODEL_ZOO.md` and should be downloaded from the GitHub Release assets.

The Git repository does not include `.pth` files by default because the target-task checkpoints are larger than the standard GitHub file limit.

